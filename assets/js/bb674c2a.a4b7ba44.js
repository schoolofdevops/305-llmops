"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["5207"],{3338(e,t,a){a.r(t),a.d(t,{metadata:()=>s,default:()=>p,frontMatter:()=>i,contentTitle:()=>l,toc:()=>h,assets:()=>c});var s=JSON.parse('{"id":"m10-observability/quiz","title":"Quiz: Observability \u2014 Metrics, Traces & the Cost of Tokens","description":"Check your grasp of the module: metrics versus traces (the ward chart and the case file), the two-phase metric model, why one P50 lies on a model service, which metric family answers which question, what a trace carries and the hygiene decision, tokens as per-request cost, and the service-link crash.","source":"@site/docs/m10-observability/quiz.mdx","sourceDirName":"m10-observability","slug":"/m10-observability/quiz","permalink":"/305-llmops/docs/m10-observability/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Observability \u2014 Metrics, Traces & the Cost of Tokens"},"sidebar":"courseSidebar","previous":{"title":"Lab: See the Platform \u2014 Metrics, the Dashboard Wall, and the Trace Waterfall","permalink":"/305-llmops/docs/m10-observability/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/305-llmops/docs/m10-observability/deep-dive"}}'),o=a(4848),n=a(8453),r=a(7517);let i={sidebar_position:3,title:"Quiz: Observability \u2014 Metrics, Traces & the Cost of Tokens"},l="Observability \u2014 Quiz",c={},h=[];function d(e){let t={h1:"h1",header:"header",p:"p",...(0,n.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"observability--quiz",children:"Observability \u2014 Quiz"})}),"\n",(0,o.jsx)(t.p,{children:"Check your grasp of the module: metrics versus traces (the ward chart and the case file), the two-phase metric model, why one P50 lies on a model service, which metric family answers which question, what a trace carries and the hygiene decision, tokens as per-request cost, and the service-link crash."}),"\n",(0,o.jsx)(r.A,{questions:[{prompt:'A product manager says OpsMate "feels slow this week" and wants to know if it is real and where the slowness is. Why is a single latency number (one P50) a poor answer for a model service specifically?',options:[{text:"A model request has two phases with two different latencies \u2014 prefill (TTFT, driven by prompt length) and decode (ITL, driven by answer length) \u2014 so one blended number hides which phase is actually slow.",correct:!0,explanation:'This is the core of the metric model. "Slow" for a model service is always one of two different slows; averaging prefill and decode into one number tells you it moved but not which phase to fix. You read the prompt-tokens rate and the decode-tokens rate separately.'},{text:"P50 is always wrong; you should report the mean instead.",correct:!1,explanation:"The mean has the same problem \u2014 it blends the two phases and is even more sensitive to the slow tail. The fix is per-phase (and per-traffic-class) reading, not a different single statistic."},{text:"Latency does not matter for model services, only throughput does.",correct:!1,explanation:"Latency absolutely matters \u2014 TTFT is how long before the first word appears. The point is not that latency is irrelevant; it is that one number blends two phases that need separate targets."},{text:"Model servers do not expose latency metrics, so P50 cannot be computed.",correct:!1,explanation:"They do expose the phase signals (prompt/decode token rates, timings). The issue is not availability of data; it is that collapsing it to one median hides the phase and the tail."}]},{prompt:"OpsMate serves a mix of many short questions and a few long ones (a pasted 2000-token incident timeline). Your dashboard shows overall P50 = 1.3s, comfortably under a 2s SLO \u2014 yet the senior engineers' long requests all take ~9s. What is going on?",options:[{text:'The median lands among the many short requests, so the slow long requests sit in the tail where the P50 cannot see them. The SLO is "green" while the requests that matter most are all failing it.',correct:!0,explanation:"Exactly the P50 lie. On mixed traffic the median is not a summary, it is a hiding place. The long prompts are a long prefill; you catch them by reading the tail and the per-phase (prompt tok/s) signal, not the median."},{text:"The long requests are being dropped, which is why they do not affect the P50.",correct:!1,explanation:"They are not dropped \u2014 they complete, in ~9s. They just do not move the median because they are a minority; they live in the tail (p95/p99), not at p50."},{text:"The dashboard is broken; a correct P50 would be ~9s.",correct:!1,explanation:"The P50 of a set that is mostly ~1.3s requests IS ~1.3s. That is arithmetically correct \u2014 and precisely why a single median is the wrong instrument for mixed model traffic."},{text:"The long requests are slow because of decode, so prefill metrics would not help.",correct:!1,explanation:"A long PROMPT is a long prefill (reading all those tokens), which is where a long request's time goes before the first token. The prompt-tokens-rate panel is exactly what reveals it."}]},{prompt:"You want to know why OpsMate feels slow even though the model itself looks healthy. Which metric family most directly tells you requests are WAITING (queued) rather than being slowly processed?",options:[{text:'llamacpp:requests_deferred \u2014 the count of requests waiting for a slot; it climbs under load while decode tok/s holds, which reads as "queueing, not a slow model".',correct:!0,explanation:"Correct. deferred is the queue. When it climbs while throughput holds, the request is not slow \u2014 it is waiting. This is also the exact signal an autoscaler acts on in M11."},{text:"llamacpp:prompt_tokens_total \u2014 the lifetime count of prompt tokens read.",correct:!1,explanation:"That is a cumulative cost counter (raw material for the bill), not a queue signal. It tells you how many prompt tokens you have processed ever, not how many requests are waiting now."},{text:'up{job="opsmate-model"} \u2014 whether the scrape target is reachable.',correct:!1,explanation:"up tells you Prometheus can reach the target (1 or 0). It says nothing about queueing; a perfectly reachable engine can have a deep queue."},{text:"http_requests_total \u2014 the generic HTTP request counter.",correct:!1,explanation:"This generic family counts HTTP calls but knows nothing about LLM slots or queueing. Reaching for the LLM-specific requests_deferred instead of the generic counter is the M10 skill."}]},{prompt:"You open an ask trace in Phoenix and click the root span. Which statements about what a trace carries \u2014 and the hygiene decision \u2014 are true? (Select all that apply.)",options:[{text:"The span's input.value attribute holds the user's prompt text verbatim, so traces can leak sensitive input by default.",correct:!0,explanation:"Correct. input.value is the raw question. Useful for debugging a bad answer, and a privacy hazard for sensitive domains \u2014 the same feature is both. Logging it is a decision, not a default."},{text:"Redacting input.value drops the prompt content from the trace while keeping the span's structure \u2014 token counts, durations, source count \u2014 so you can still debug performance without storing the text.",correct:!0,explanation:"Correct. Redaction is a structure filter: the journey is still recorded (the register), the content is not (the confession). OpsMate exposes this as the OTEL_REDACT_INPUT toggle."},{text:"The generate span carries llm.token_count.prompt/completion/total, lifted from the model's usage block \u2014 the per-request token cost.",correct:!0,explanation:"Correct. The engine returns a usage block; the app puts it on the LLM span. Summing llm.token_count.total across a day's traces is your token bill, sliced however you grouped the spans."},{text:"Redacting input.value encrypts the request end-to-end, so it is a complete PII protection on its own.",correct:!1,explanation:"False \u2014 redaction only drops the content from the TRACE. It does not touch the request in flight or the model's own logs. Real PII protection is defense in depth; M12 handles the harder cases at the gateway. M10's job is to make you see the data and decide."}],multiSelect:!0},{prompt:"You apply observability.yaml and the Phoenix pod is in CrashLoopBackOff. The log says: PHOENIX_PORT was set to 'tcp://10.96.148.31:6006', which is not a valid port. What happened, and what is the fix?",options:[{text:'The Service named "phoenix" made Kubernetes inject a service-link env PHOENIX_PORT=tcp://\u2026, which collides with Phoenix\'s own PHOENIX_PORT config. Set PHOENIX_PORT explicitly (or enableServiceLinks: false) so your value wins.',correct:!0,explanation:'Correct \u2014 the designed M10 exhibit. Kubernetes injects <NAME>_PORT service-link envs for every Service; when the name matches a config var the app reads, it collides. The narrow fix is an explicit PHOENIX_PORT: "6006"; the broad fix is disabling service links.'},{text:"Phoenix cannot bind port 6006 because Grafana already took it, so you must change Phoenix's port.",correct:!1,explanation:"There is no port conflict \u2014 Grafana is on 3000. Phoenix is receiving a tcp://\u2026 STRING where it expects an integer, from a service-link env, not losing a bind race."},{text:"The image tag is wrong; pull a different Phoenix version.",correct:!1,explanation:"The image is fine. The crash is a config-parse error from an injected env var, reproducible on any version that reads PHOENIX_PORT \u2014 changing the tag does not remove the service-link injection."},{text:"Phoenix ran out of memory; raise its limit above 768Mi.",correct:!1,explanation:"An OOM shows as OOMKilled, not a ValueError about parsing a port string. The log names the exact cause and the exact fix \u2014 read it rather than raising limits."}]},{prompt:"What is the right division of labour between the Grafana metrics dashboard and the Phoenix traces when you are handed the three Monday questions (is it slow? why did the bill move? where did the 11 seconds go)?",options:[{text:'Metrics (ward chart) answer "is it slow / is the queue building" across the whole service over time; a trace (case file) answers "where did THIS request spend its time and what did it cost". You use both, each for its own question.',correct:!0,explanation:"Correct. Metrics are sampled aggregates over time (the trend, the queue); a trace is one request's full timeline with per-request token cost. The dashboard cannot explain one slow request; a single trace cannot tell you the service is trending badly."},{text:"Traces replace metrics entirely, since a trace has everything a metric has plus more detail.",correct:!1,explanation:"They do not substitute. A trace is one request; it cannot show a service-wide trend or a growing queue. You keep the metrics for the aggregate view."},{text:"Metrics answer all three questions; traces are only for debugging crashes.",correct:!1,explanation:'Metrics cannot answer "where did the 11 seconds go in this request" \u2014 that needs the per-span timeline. Traces are for the case-file questions, not only crashes.'},{text:"The bill question can only be answered by the cloud provider's invoice, not by your own telemetry.",correct:!1,explanation:"The token cost is on the generate span (llm.token_count.*). Summing it across traces gives you the bill, broken down by user or prompt length \u2014 before the invoice arrives."}]},{prompt:"In the lab, one Prometheus query needed curl --data-urlencode while another worked fine passed raw in the URL. Which query needed the encoding, and why?",options:[{text:'up{job="opsmate-model"} needed it, because the label matcher\'s {, ", and } are not URL-safe \u2014 passed raw they reach Prometheus malformed and it returns an empty (null) result. The bare metric name llamacpp:prompt_tokens_total has no special characters, so it works as-is.',correct:!0,explanation:'Correct. The braces and quotes of a label matcher must be encoded or the query arrives broken and silently returns nothing \u2014 a null result that looks like "no data" but is really a malformed query. A bare metric name is plain text and needs no encoding.'},{text:"llamacpp:prompt_tokens_total needed it, because a counter name contains a colon that must be percent-encoded.",correct:!1,explanation:"The colon in llamacpp:prompt_tokens_total is fine in a query string; that query is passed raw in the lab and works. The encoding is about the label matcher's braces and quotes, not the metric name."},{text:"Both queries needed it; the lab only shows one for brevity.",correct:!1,explanation:"Only the label-matcher query needs it. The lab deliberately passes the second (bare metric name) query in the URL as-is to make exactly this point: encoding is needed for the special characters of a matcher, not for every query."},{text:"Neither actually needs it; --data-urlencode is a stylistic preference.",correct:!1,explanation:'It is not stylistic \u2014 without it the matcher query returns null, which the lab calls out as a real failure mode (an empty result that masquerades as "no data").'}]},{prompt:"The Deep Dive maps metric concepts across engines because OpsMate's serving engine is meant to swap (Ollama \u2192 llama.cpp \u2192 vLLM). Which statements about moving a dashboard between llama.cpp and vLLM are true? (Select all that apply.)",options:[{text:"A dashboard that hardcodes one engine's family names (e.g. llamacpp:requests_deferred) goes blank against another engine, even though the underlying concept (queued requests) is the same \u2014 the fix is to build around the concept and re-map the family per engine.",correct:!0,explanation:'Correct. The concept "requests queued" is llamacpp:requests_deferred on llama.cpp and vllm:num_requests_waiting on vLLM. The panel measures the concept; the exact query changes on a swap. A dashboard is not portable for free.'},{text:"vLLM exposes true histograms (e.g. vllm:time_to_first_token_seconds), so the per-phase SLO becomes a first-class histogram_quantile query, whereas on llama.cpp you approximate TTFT from the prompt-processing timing.",correct:!0,explanation:"Correct. Because vLLM hands you a real TTFT histogram, the SLO-per-phase discipline from the lesson gets easier on vLLM \u2014 you query genuine percentiles instead of approximating from a gauge/counter."},{text:"KV-cache utilization is a vLLM gauge (vllm:gpu_cache_usage_perc) with no direct llama.cpp equivalent, which is why the M10 CPU dashboard uses the queue (requests_deferred) as its saturation proxy instead.",correct:!0,explanation:"Correct. vllm:gpu_cache_usage_perc is the M1 KV-cache budget as a live percentage \u2014 the top saturation signal on a GPU server. llama.cpp on CPU does not surface it the same way, so the dashboard falls back to the queue depth."},{text:"Because the endpoint contract holds across engines, the Prometheus metric names are identical too, so a dashboard moves between engines with zero changes.",correct:!1,explanation:"False. The OpenAI-compatible endpoint contract keeps client code unchanged, but the /metrics family names differ per engine (llamacpp:* vs vllm:*). The dashboard queries must be re-mapped even though client code is untouched."}],multiSelect:!0},{prompt:"The Deep Dive argues that for OpsMate's production LLM traffic you should prefer tail sampling over head sampling. What is the core reason?",options:[{text:"Head sampling decides before the request runs (a blind coin flip), so it is blind to how the request turned out \u2014 it keeps 5% of boring requests and drops 95% of the slow, expensive, and errored ones. Tail sampling decides after completion, when you know duration/tokens/error, so it can keep exactly the rare heavy-tailed requests you need the case file for.",correct:!0,explanation:"Correct. LLM traffic is heavy-tailed: the eleven-second, eight-thousand-token request is rare and is the one you are on call for. A 5% head sample would throw it away; tail sampling keeps the slow/expensive/errored traces and drops the boring bulk."},{text:"Tail sampling is cheaper to run because the keep-or-drop decision needs no buffering.",correct:!1,explanation:"Backwards \u2014 tail sampling costs more to run, because you must hold the spans until the request completes before deciding. That is why it runs in an OTel Collector (tailsamplingprocessor) between the app and the backend, not in the app itself."},{text:"Head sampling cannot be configured in OpenTelemetry, so tail sampling is the only option.",correct:!1,explanation:"Head sampling is fully supported \u2014 the default TraceIdRatioBased sampler does exactly that. The choice is about which requests you keep, not about what is available."},{text:"Tail sampling encrypts sensitive spans, which head sampling cannot do.",correct:!1,explanation:"Sampling is about which traces to keep, not encryption or redaction. Content hygiene (redacting input.value) is a separate control from the head-vs-tail keep decision."}]},{prompt:"The lab has you turn on OTEL_REDACT_INPUT and re-ask, then check the newest ask span. What exactly changes in the trace, and what does that buy you?",options:[{text:"input.value now reads [redacted] instead of the prompt text, but the span's structure \u2014 token counts, durations, source count \u2014 is unchanged, so you can still debug performance and cost without storing the sensitive content.",correct:!0,explanation:"Correct. Redaction is a structure filter: the register still records that a request retrieved 3 documents and took N seconds; it just does not record the confession (the prompt text). You keep the debuggable structure and drop the content."},{text:"The whole ask span disappears from Phoenix, so there is no longer a trace to debug.",correct:!1,explanation:"The span stays \u2014 the token counts, durations, and source count are all still there. Only input.value is replaced with [redacted]. Redaction removes content, not the span."},{text:"The prompt is encrypted at rest in Phoenix, so an operator with Phoenix access still cannot read it.",correct:!1,explanation:"Redaction is not encryption \u2014 the lab is explicit that it is a structure filter, one telemetry-hygiene layer. It drops the content from the trace; it does not touch the request in flight or the model's own logs, and there is no encryption involved."},{text:"Redaction also strips the prompt from the model server's logs and from the request in flight, making it complete PII protection.",correct:!1,explanation:"False \u2014 redaction only affects the trace attribute. The model's own logs and the in-flight request are untouched. Real PII protection is defense in depth; M12 handles the harder cases at the gateway."}]},{prompt:'When you drive the M3 load test against the single engine and watch the Grafana wall, requests_deferred climbs while decode tok/s holds roughly steady. Why does this pattern point to a different fix than "the model is slow"?',options:[{text:"A climbing deferred gauge with steady decode throughput means requests are waiting for a slot, not being processed slowly \u2014 the engine is queueing, not degraded. The fix is to add an engine (the M11 autoscaling story), not to change the model or hardware.",correct:!0,explanation:'Correct. deferred is the queue; when it grows while throughput holds, the engine is batching at its rate and the extra requests are simply waiting. That is a capacity problem (add a replica) not a latency problem (slower model). A single p95 number would show only "latency went up" and leave you guessing.'},{text:"It means the model is genuinely slower under load, so you should switch to a smaller model.",correct:!1,explanation:"Decode tok/s holding steady is the tell that the model is NOT slower \u2014 it keeps generating at its rate. The requests are waiting in the queue, which is a capacity issue, not a per-token speed issue."},{text:"It means Prometheus is failing to scrape, so the deferred number is unreliable.",correct:!1,explanation:"The scrape is healthy (up = 1 in the lab); deferred climbing is a real signal, not a scrape artifact. It is the queue forming under concurrency exactly as designed."},{text:"A steady decode rate proves there is no queue, so the slowness must be network latency.",correct:!1,explanation:"Steady decode with a rising deferred gauge is precisely the signature of a queue forming \u2014 throughput holds because the engine batches, while excess requests wait. It is queueing, not a network problem."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,n.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>r});var s=a(4848),o=a(6540);function n({q:e,state:t,onToggle:a}){return(0,s.jsxs)("div",{className:"question_QpiT",children:[(0,s.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,s.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,s.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let n=t.selected.has(o),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":n&&(r+=" incorrect_kRFc"):n&&(r+=" chosen_H3WG"),(0,s.jsxs)("li",{children:[(0,s.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>a(o),children:[(0,s.jsx)("span",{className:"marker_vN_w",children:n?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(n||e.correct)&&e.explanation&&(0,s.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function r({questions:e}){let[t,a]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),i=t.every(e=>e.checked),l=t.filter((t,a)=>{var s,o;let n;return s=e[a],o=t.selected,n=s.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),s.multiSelect?o.size===n.length&&[...o].every(e=>s.options[e].correct):1===o.size&&n.includes([...o][0])}).length;return(0,s.jsxs)("div",{className:"quiz_I014",children:[e.map((o,r)=>(0,s.jsx)(n,{q:o,state:t[r],onToggle:t=>a(a=>a.map((a,s)=>{if(s!==r)return a;let o=new Set(a.selected);return e[r].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...a,selected:o}}))},r)),(0,s.jsxs)("div",{className:"actions_XUwt",children:[(0,s.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,s.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),i&&(0,s.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>r,x:()=>i});var s=a(6540);let o={},n=s.createContext(o);function r(e){let t=s.useContext(n);return s.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function i(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),s.createElement(n.Provider,{value:t},e.children)}}}]);