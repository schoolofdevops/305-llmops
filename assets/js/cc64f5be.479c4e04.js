"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["3944"],{8158(e,t,a){a.r(t),a.d(t,{metadata:()=>n,default:()=>p,frontMatter:()=>i,contentTitle:()=>l,toc:()=>c,assets:()=>h});var n=JSON.parse('{"id":"m9-serving-patterns/quiz","title":"Quiz: Serving Patterns \u2014 Canary Routing, KServe & llm-d","description":"Check your grasp of the module: the healthy-pod-wrong-model trap, reading a split at the right sample size, the quality-vs-crash signal, staged promotion, the kit-filename lesson, and when to climb past the router to KServe or llm-d.","source":"@site/docs/m9-serving-patterns/quiz.mdx","sourceDirName":"m9-serving-patterns","slug":"/m9-serving-patterns/quiz","permalink":"/305-llmops/docs/m9-serving-patterns/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Serving Patterns \u2014 Canary Routing, KServe & llm-d"},"sidebar":"courseSidebar","previous":{"title":"Lab: Canary a Model on Quality \u2014 Router, Golden-Set Gate, Rollback","permalink":"/305-llmops/docs/m9-serving-patterns/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/305-llmops/docs/m9-serving-patterns/deep-dive"}}'),o=a(4848),s=a(8453),r=a(7517);let i={sidebar_position:3,title:"Quiz: Serving Patterns \u2014 Canary Routing, KServe & llm-d"},l="Serving Patterns \u2014 Quiz",h={},c=[];function d(e){let t={h1:"h1",header:"header",p:"p",...(0,s.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"serving-patterns--quiz",children:"Serving Patterns \u2014 Quiz"})}),"\n",(0,o.jsx)(t.p,{children:"Check your grasp of the module: the healthy-pod-wrong-model trap, reading a split at the right sample size, the quality-vs-crash signal, staged promotion, the kit-filename lesson, and when to climb past the router to KServe or llm-d."}),"\n",(0,o.jsx)(r.A,{questions:[{prompt:"Your canary pod is 1/1 Running, the readiness probe is green, and every request returns a 200 with a clean usage block. Why is that NOT enough to promote the new model version?",options:[{text:"A readiness probe checks that the server answers, not that its answers are any good \u2014 the pod can be healthy while the model is worse.",correct:!0,explanation:'This is the module\'s core trap: "the pod is Running" is a crash signal; it says nothing about answer quality. A readiness probe cannot taste the food. Only running your evals against the model catches a healthy pod serving a bad model.'},{text:"A 200 response means the request failed silently; you need a 201 for a successful completion.",correct:!1,explanation:"A 200 with a usage block is a successful completion. The problem is not the HTTP status \u2014 it is that a correct-looking response can still be a worse answer."},{text:"The readiness probe only fires once, so it cannot reflect the current state of the model.",correct:!1,explanation:"Readiness probes run continuously. The issue is not staleness \u2014 it is that readiness measures liveness/traffic-eligibility, not answer quality, which no probe can measure."},{text:"It is enough \u2014 a green readiness probe on a model server is a reliable proxy for correct answers.",correct:!1,explanation:'This is exactly the assumption the module breaks. For an ordinary web service "healthy" is a decent proxy for "works"; for a model server it is not, because the model can regress while the pod stays perfectly healthy.'}]},{prompt:"You set the canary to 10%, send 20 requests through the router, and count 0 canary hits from the X-Served-By header. What is the right conclusion?",options:[{text:"Nothing is wrong \u2014 a 10% canary gets zero hits in 20 requests about 12% of the time (0.9^20). 20 is too few to read a 10% split; re-read it at n=100.",correct:!0,explanation:"Correct. Reading a split needs enough samples: 0.9^20 \u2248 0.12, so ~1 in 8 runs shows zero canary at n=20 purely by chance. At n=100 the ~90/10 pattern becomes legible. Judging a split from 20 requests is judging a coin from three flips."},{text:"The split is broken \u2014 restart the router until you see canary hits at n=20.",correct:!1,explanation:"Restarting to chase a small-sample result hides nothing and fixes nothing. The split is fine; 20 requests simply cannot resolve a 10% weight. Increase the sample size, do not restart."},{text:"The canary pod must be down, because a working canary always appears within 20 requests.",correct:!1,explanation:"A 10% canary has no guarantee of appearing in 20 requests. Confirm the pod with kubectl get pods if unsure, but zero hits at n=20 is expected variance, not evidence the pod is down."},{text:"The weights are being applied in reverse (90% to canary), which is why you see none.",correct:!1,explanation:"Reversed weights would show ~90% canary, not zero. Zero canary at n=20 is the small-sample illusion, consistent with the correct 10% weight."}]},{prompt:"The canary Deployment applied cleanly, the initContainer finished, but the llama-server container is in CrashLoopBackOff. The log says: failed to load model '/model/models/gguf/qwen3-0.6b-q8_0.gguf'. What is the fix?",options:[{text:"The -m arg points at the BASE filename, but this kit is the candidate, whose model layer unpacks to a different file (opsmate-tuned-q8_0.gguf). Point -m at the candidate's actual filename.",correct:!0,explanation:"Correct \u2014 this is the designed exhibit. Every ModelKit carries its own packed filename; a copy-pasted base manifest points at the wrong file. Read the fetch-model log to see what actually landed on the PVC, then set -m to match."},{text:"The initContainer failed to pull the model, so the file is missing \u2014 fix the registry hostAliases IP.",correct:!1,explanation:"The initContainer FINISHED and the fetch-model log shows the file on the PVC \u2014 the pull worked. The server is looking at the wrong path, not at a missing pull. hostAliases would be the fix for an init failure, which this is not."},{text:"The model is corrupt; re-pack and re-push the candidate kit.",correct:!1,explanation:"The model loaded fine as the candidate's own file; the server just was not told that name. Re-packing does not change the filename mismatch in the -m arg."},{text:"The memory limit is too low; raise limits.memory so the model can load.",correct:!1,explanation:"An OOM shows as OOMKilled, not \"failed to load model '<path>'\". This error is a missing file at the given path, not a memory ceiling."}]},{prompt:"Which of these are true about the quality gate (canary_gate.py) and how it drives promote/rollback? (Select all that apply.)",options:[{text:"It runs the golden set against the canary AND the base, and the candidate must at least tie the base to earn promotion.",correct:!0,explanation:'Correct. The candidate is replacing a model that already works, so "no worse than the incumbent" is the floor. The gate compares pass-counts on the same golden questions against both backends.'},{text:"Promotion and rollback become data verbs \u2014 decided by the eval score \u2014 not crash verbs decided by whether the pod fell over.",correct:!0,explanation:'Correct, and it is the point of the module. "It crashed, roll it back" is the weak signal; "the golden set says it is worse, roll it back" is the signal that catches a healthy-but-worse model.'},{text:"A ROLLBACK verdict is executed by editing the split_clients weight to 0% and running kubectl rollout restart on the router.",correct:!0,explanation:"Correct. Rollback is a weight edit plus a rollout restart so nginx re-reads the ConfigMap \u2014 the same manual, visible mechanic as any weight change."},{text:"Because the gate hits the models directly on /v1, a green readiness probe on the canary would make the gate pass automatically.",correct:!1,explanation:"False \u2014 and it is the trap the module exists to break. The gate ignores readiness entirely; it scores actual answers. The M6 candidate has a perfectly green readiness probe and STILL fails the gate because its answers regressed."}],multiSelect:!0},{prompt:"The candidate passes the gate at 10%. Why climb 10% \u2192 50% \u2192 100% in stages rather than jumping straight to 100%?",options:[{text:"Each level bounds the blast radius and is gated on the golden set, so a regression that only shows at higher traffic is caught before every user is on the new version.",correct:!0,explanation:"Correct \u2014 the school-van ladder. Staged promotion limits how many users a bad version can reach at each step and gives the gate another checkpoint, so you can demote a level the moment it goes red."},{text:"nginx split_clients cannot express 100% in a single step, so you must climb in increments.",correct:!1,explanation:"split_clients handles any percentage including 100% directly. The staging is a risk discipline, not a tooling limitation."},{text:"Jumping to 100% would delete the base Deployment automatically.",correct:!1,explanation:"Re-weighting the router changes traffic only; it does not delete the base Deployment. The base pod keeps running regardless of the weight."},{text:"The KV cache must warm up gradually, which only happens if traffic increases slowly.",correct:!1,explanation:"Cache warming is not why you stage a promotion. You stage it to bound risk and keep re-measuring quality at each traffic level."}]},{prompt:"When does climbing past the M9 nginx router to KServe or llm-d actually pay for its dependency cost? (Select all that apply.)",options:[{text:"KServe pays when you run a FLEET of models and want scale-to-zero and a standardized rollout \u2014 enough to justify the Knative + networking + cert-manager stack.",correct:!0,explanation:"Correct. KServe's canaryTrafficPercent and scale-to-zero are real upgrades, but they ride a dependency stack that only pays across a fleet, not for one always-on model on a small cluster."},{text:"llm-d / the Gateway API Inference Extension pays when the routing decision itself must be KV-cache- and prefix-aware at scale (e.g. disaggregated prefill/decode pools).",correct:!0,explanation:"Correct. The frontier exists because the router cannot do per-token or prefix-aware routing; you climb to it when cache affinity and right-sized prefill/decode scaling actually move your cost or latency."},{text:"You should adopt KServe immediately for any canary, because a declared canaryTrafficPercent field is always better than an nginx ConfigMap.",correct:!1,explanation:"False \u2014 this ignores the dependency cost. For one split on a laptop, the 64Mi nginx is the honest choice; KServe's stack would be larger than the problem. Climb the ladder only when the problem forces you."},{text:"You reach for llm-d whenever you have two model versions, since any canary needs cache-aware routing.",correct:!1,explanation:"False. A plain weight split (the M9 router) canaries two versions fine. Cache-aware routing pays only at a scale where prefix affinity and disaggregation matter \u2014 not for the mere fact of having two versions."}],multiSelect:!0},{prompt:"Both the base and the canary backend serve the same model alias (qwen3-0.6b) under the same OpenAI /v1 contract. What is the design reason, and how do you tell which one actually answered a request?",options:[{text:"The identical contract means the client cannot tell them apart and never has to change; you distinguish the backends by the X-Served-By response header, not by the model field.",correct:!0,explanation:"Correct. The endpoint contract that held from laptop to cluster now holds across two versions \u2014 the router splits, the client is unchanged. The per-request X-Served-By header is the observability hook that names the backend so you can watch the split instead of trusting it."},{text:"They must serve different model names so the router can tell them apart; X-Served-By is only for logging.",correct:!1,explanation:"The opposite is true. Both deliberately use the same alias precisely so the client cannot distinguish them. The router routes by weight, not by model name, and X-Served-By \u2014 not the model field \u2014 is what identifies the backend."},{text:"Same alias is a bug in the manifests; each backend should expose a unique model id so clients can pin a version.",correct:!1,explanation:"It is intentional, not a bug. Pinning a version at the client would defeat the whole point of a transparent canary \u2014 the client should be unaware of which version served it."},{text:"You read the .model field in the JSON response body to see whether base or canary answered.",correct:!1,explanation:"The .model field returns the shared alias for both, so it cannot distinguish them. The distinguishing signal is the X-Served-By header the router adds."}]},{prompt:"The gate sends the base model a generous max_tokens budget (1024). Why does the base specifically need that headroom, when the tuned candidate does not?",options:[{text:"The base is a reasoning model that writes a <think> pass before answering; at a tight budget it would spend the whole allowance thinking, return an empty answer, and score zero on every net \u2014 flipping the verdict to a false PROMOTE.",correct:!0,explanation:"Correct. The gate scores the final answer, not the scratchpad, and gives both models the same generous budget so the base can finish reasoning AND answer. The tuned candidate answers directly with no reasoning pass, so it is not the one that needs the headroom."},{text:"The base is larger, so each token takes more memory and it needs a bigger buffer to avoid OOM.",correct:!1,explanation:"The headroom is about token count for the reasoning pass, not memory per token. Both models are the same 0.6B size; the difference is that the base thinks before it answers."},{text:"A larger budget makes the base answer more accurately, tilting the gate in its favor deliberately.",correct:!1,explanation:"The budget is not a thumb on the scale \u2014 it exists so the base is scored on a real answer rather than a truncated reasoning pass. Starving it would produce a false PROMOTE, the opposite of favoring the base unfairly."},{text:"The candidate needs the same budget for its own <think> pass; the base is just a fallback.",correct:!1,explanation:"The tuned candidate answers directly, without a reasoning pass, so it does not consume budget on thinking. It is the base \u2014 the reasoning model \u2014 that needs the room."}]},{prompt:"A colleague proposes adopting KServe for a single, always-on OpsMate model on your small cluster because canaryTrafficPercent looks cleaner than an nginx ConfigMap. Using the decision tree and the deep dive, what is the correct pushback?",options:[{text:"KServe rides on Knative Serving, a networking layer (Istio/Kourier), and cert-manager \u2014 three subsystems to install, run, and patch. For one always-on model that dependency stack is larger than the one split you need; a 64Mi nginx is the honest choice until a fleet plus scale-to-zero justifies KServe.",correct:!0,explanation:"Correct. The decision tree is a ladder of capability against a ladder of dependency cost; you climb only when the problem forces you. KServe pays for a fleet with scale-to-zero and standardized rollout, not for a single model where its stack is pure overhead."},{text:"Agree immediately \u2014 a declared canaryTrafficPercent field is always superior to a ConfigMap, regardless of cluster size.",correct:!1,explanation:"This ignores the dependency cost, which is exactly the trap the module warns against. For one split on small hardware, the nginx router is right; KServe's stack would outweigh the problem."},{text:"Reject KServe permanently \u2014 it can never canary correctly because canaryTrafficPercent is coarser than split_clients.",correct:!1,explanation:"KServe is not worse at canarying \u2014 its canaryTrafficPercent replaces your two split_clients lines and adds scale-to-zero. The point is not that KServe is bad, but that its dependency stack only pays across a fleet."},{text:"Adopt KServe but skip Knative and cert-manager to keep it light.",correct:!1,explanation:"You cannot skip them \u2014 KServe rides on Knative Serving and needs cert-manager for its webhook certs. Those are the dependencies that make the stack heavy; there is no light-mode KServe here."}]},{prompt:"From the deep dive: which capabilities can the frontier (llm-d / Gateway API Inference Extension) provide that the M9 nginx router structurally CANNOT? (Select all that apply.)",options:[{text:"Prefix-affinity routing \u2014 sending a request to the pod that already holds its prompt's prefix KV cache, so a shared system prompt is not re-encoded on every request.",correct:!0,explanation:"Correct. split_clients hashes a request id it cannot interpret and has no view of what any pod holds, so it cannot reuse a warm prefix cache. The endpoint-picker routes on inference state, reclaiming that recompute \u2014 real waste for a workload like OpsMate with one long shared system prompt."},{text:"Scale-to-zero \u2014 cold-starting a model that has released its node, instead of returning 502 when a backend has no ready pod.",correct:!0,explanation:"Correct. An nginx upstream is a static list of Services; a backend with no ready pod is a 502, not a queued request. KServe/Knative and the frontier can hold zero replicas and cold-start on demand."},{text:"Disaggregated prefill and decode \u2014 running the compute-bound prefill and bandwidth-bound decode phases as two independently-scaled pools with a KV-cache handoff between them.",correct:!0,explanation:"Correct, and it is the M1 call-back made physical. Prefill and decode have opposite bottlenecks, so packing them on one pod sizes neither right; llm-d runs them as two pools each scaled for its own limit. The router splits whole requests by weight and cannot do this."},{text:"Splitting a fixed 90/10 weight between two versions, which nginx cannot do at all.",correct:!1,explanation:"False \u2014 a fixed weight split between two versions is exactly what the M9 router DOES do, and does well. That is the router's home turf, not a frontier-only capability."}],multiSelect:!0},{prompt:'You edit router.yaml to drop the canary to 0%, apply it, but the split still reads canary hits. Separately, you tried writing a literal 0% canary bucket and the router went CrashLoopBackOff with invalid percent value "0%". What are the two lessons?',options:[{text:"A pod does not reload a mounted ConfigMap on its own \u2014 you must kubectl rollout restart the router after apply for nginx to re-read the split; and split_clients rejects a literal 0% bucket, so zero canary traffic is expressed by removing the canary line so everything falls through to the * (base) bucket.",correct:!0,explanation:"Correct on both. The always-apply \u2192 rollout restart \u2192 re-read discipline is what makes an edit take effect; forgetting the restart leaves nginx serving the old config. And because split_clients refuses a 0% bucket, you remove the canary line rather than set it to 0%."},{text:"Apply alone reloads the ConfigMap; the stale split means the apply failed, and 0% is a valid bucket that just needs a restart to register.",correct:!1,explanation:"Both halves are wrong. A mounted ConfigMap does not hot-reload into a running pod \u2014 the rollout restart is required. And a literal 0% bucket is not valid at all; it is why the router CrashLooped."},{text:"You must scale the canary Deployment to zero replicas to route 0% traffic; the ConfigMap plays no part.",correct:!1,explanation:"Scaling the canary to zero would make the router 502 if it ever routed there \u2014 the weight, expressed in the ConfigMap by removing the canary line, is what routes zero traffic. The ConfigMap and the restart are exactly the mechanism."},{text:"The literal 0% error means nginx is broken; downgrade the image to accept 0% buckets.",correct:!1,explanation:"It is not a broken image \u2014 split_clients by design will not accept a 0% bucket. The supported way to express zero is to drop the canary line so traffic falls through to the base bucket."}]},{prompt:"The lab has you read the split at n=20 (which lies) and again at n=100 (which tells the truth) before running the gate. Which statements about reading a split honestly are correct? (Select all that apply.)",options:[{text:"A 10% canary getting zero hits in 20 requests happens about 12% of the time (0.9^20), so zero canary at n=20 is expected variance, not a broken split.",correct:!0,explanation:"Correct. Roughly 1 run in 8 shows zero canary at n=20 purely by chance. Restarting the router to chase that is treating expected sampling noise as a bug."},{text:"At n=100 the observed split converges near the configured weights (~90/10), close enough to read even though the exact numbers vary run to run.",correct:!0,explanation:"Correct. More samples resolve the weight: at n=100 a 90/10 split lands around 87/13 or 92/8 \u2014 a random draw, but legible. The discipline is to measure a split at a sample size that can actually show it."},{text:"The n=20 versus n=100 point is only about health checks; it does not generalize to how you should read the quality gate's results.",correct:!1,explanation:"It is not a detour \u2014 it is a canary lesson in miniature. Measuring any new version, including the gate's pass-counts, needs enough samples to be real; twenty requests lie the same way a three-flip coin does."},{text:"If n=20 shows zero canary hits, the correct move is to restart the router until canary hits appear at n=20.",correct:!1,explanation:"Restarting to force a small-sample result fixes and hides nothing. The split is fine; you increase the sample size to n=100 to read it, you do not restart."}],multiSelect:!0}]})]})}function p(e={}){let{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>r});var n=a(4848),o=a(6540);function s({q:e,state:t,onToggle:a}){return(0,n.jsxs)("div",{className:"question_QpiT",children:[(0,n.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,n.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,n.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let s=t.selected.has(o),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":s&&(r+=" incorrect_kRFc"):s&&(r+=" chosen_H3WG"),(0,n.jsxs)("li",{children:[(0,n.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>a(o),children:[(0,n.jsx)("span",{className:"marker_vN_w",children:s?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(s||e.correct)&&e.explanation&&(0,n.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function r({questions:e}){let[t,a]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),i=t.every(e=>e.checked),l=t.filter((t,a)=>{var n,o;let s;return n=e[a],o=t.selected,s=n.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),n.multiSelect?o.size===s.length&&[...o].every(e=>n.options[e].correct):1===o.size&&s.includes([...o][0])}).length;return(0,n.jsxs)("div",{className:"quiz_I014",children:[e.map((o,r)=>(0,n.jsx)(s,{q:o,state:t[r],onToggle:t=>a(a=>a.map((a,n)=>{if(n!==r)return a;let o=new Set(a.selected);return e[r].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...a,selected:o}}))},r)),(0,n.jsxs)("div",{className:"actions_XUwt",children:[(0,n.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,n.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),i&&(0,n.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>r,x:()=>i});var n=a(6540);let o={},s=n.createContext(o);function r(e){let t=n.useContext(s);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function i(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),n.createElement(s.Provider,{value:t},e.children)}}}]);